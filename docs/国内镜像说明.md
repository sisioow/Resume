# 国内镜像说明

## 重要：Gitee Pages 已下线

Gitee 已停用 Pages，仓库「服务」菜单里**不会再出现** Gitee Pages。  
请改用下面方案做国内可预览镜像。

---

## 推荐：Cloudflare Pages（免费 · 免备案）

国内多数网络可直接打开，不会像阿里云 OSS 默认域名那样强制下载。

### 步骤（约 3 分钟）

1. 打开 https://dash.cloudflare.com/ 免费注册并登录  
2. 左侧 **Workers & Pages** → **Create** → **Pages** → **Connect to Git**  
3. 授权 GitHub，选择仓库 **`sisioow/Resume`**  
4. 构建设置填写：

| 项 | 值 |
|----|-----|
| Framework preset | Vite |
| Root directory | `site` |
| Build command | `npm run build` |
| Build output directory | `dist` |

5. **Save and Deploy**，等待完成  
6. 得到地址，形如：`https://xxxx.pages.dev`

把该地址发我，我会写入站点页脚「国内镜像」。

之后每次推送 GitHub `main`，Cloudflare 会自动重新构建。

---

## 其他渠道

| 渠道 | 说明 |
|------|------|
| GitHub Pages | https://sisioow.github.io/Resume/ · 海外稳定，国内可能需加速 |
| 阿里云 OSS | 仅备份；默认域名访问 HTML 会**强制下载** |
| OSS + 备案域名 | 有备案域名时可绑定后正常预览 |

Gitee 仓库里的 `gh-pages` 分支可以忽略，不再用于站点发布。

## DeepSeek 真实演示

演示页通过 Cloudflare Function `/api/chat` 调用 `deepseek-v4-pro`（秘钥不进前端）。配置见 [deepseek-demo.md](./deepseek-demo.md)。
